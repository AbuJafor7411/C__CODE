#include<stdio.h>
int main()
{
    int l, w, area;
    printf("Enter length & width of the rectangle:");
    scanf("%d%d",&l,&w);
    area=l*w;
    printf("\nArea of the Rectangle is %d\n",area);
    return 0;
}
